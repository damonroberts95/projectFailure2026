---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const sketches = (await getCollection('sketches', ({ data }) => !data.draft)).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<BaseLayout title="Sketches" description="Comedy sketches about science, from Project Failure.">
	<h1>Sketches</h1>
	<p>Science history, re-enacted badly on purpose.</p>

	<ul class="grid">
		{
			sketches.map((sketch) => (
				<li class="card">
					<a href={`/sketches/${sketch.id}/`}>
						{sketch.data.ogImage ? (
							<img src={sketch.data.ogImage} alt="" loading="lazy" />
						) : (
							<div class="placeholder" aria-hidden="true" />
						)}
						<div class="card-body">
							<strong>{sketch.data.title}</strong>
							<p>{sketch.data.description}</p>
						</div>
					</a>
				</li>
			))
		}
	</ul>
</BaseLayout>

<style>
	.grid {
		list-style: none;
		padding: 0;
		margin: 2rem 0 0;
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
		gap: 1.5rem;
	}
	.card {
		background: #fff;
		border-radius: var(--radius-card);
		box-shadow: var(--shadow-card);
		overflow: hidden;
		transition: transform 0.15s ease;
	}
	.card:hover {
		transform: translateY(-3px);
	}
	.card a {
		display: block;
		color: var(--color-ink);
		text-decoration: none !important;
	}
	.card img,
	.placeholder {
		width: 100%;
		aspect-ratio: 16 / 9;
		display: block;
		object-fit: cover;
		background: var(--color-board);
	}
	.placeholder {
		background-image: radial-gradient(circle at 30% 30%, rgba(255, 255, 255, 0.08), transparent 60%);
	}
	.card-body {
		padding: 1rem 1.25rem 1.25rem;
	}
	.card-body strong {
		font-size: 1.1rem;
	}
	.card-body p {
		margin: 0.35rem 0 0;
		color: #555;
		font-size: 0.9rem;
	}
</style>
